# URL base da API Fastify (sem barra no final)
CMS_API_BASE_URL=http://localhost:3333

# JWT secrets — devem ser idênticos aos definidos no stetsom-api/.env
JWT_ACCESS_SECRET=<mesmo-valor-de-JWT_ACCESS_SECRET-no-backend>
JWT_REFRESH_SECRET=<mesmo-valor-de-JWT_REFRESH_SECRET-no-backend>
